@model IEnumerable<Category>
<div class="my-4">
    <ul class="list-group my-3 ">
        @foreach (var cat in Model)
        {
            <li class="list-group-item">
                @cat.CategoryName
            </li>
        }

    </ul>
</div>